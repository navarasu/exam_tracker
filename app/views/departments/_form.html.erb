<%= form_with model: @department do |f| %>
    <div class="modal-body px-5 py-4">
      <div class='row mb-4'>
        <div class= 'col-6'>
          <%= f.label :name, "Name" %>
          <%= f.text_field :name ,placeholder: "Enter Department Name",class:"form-control" %>
        </div>
      </div>
    </div>
  <div class="modal-footer">
    <%= f.submit "Save Department", class:"btn btn-secondary" %>
    <button type="button" class="btn btn-link text-gray-600 ms-auto" data-bs-dismiss="modal">Close</button>
  </div>
  <% end %>
  